// Fill these in from your Supabase project: Project Settings -> API.
// SUPABASE_ANON_KEY is safe to expose in a static site — it only has the
// read-only access granted by the RLS policy in supabase/schema.sql.
window.GREENFLOW_CONFIG = {
  SUPABASE_URL: "ISI_URL_SUPABASE_KAMU",
  SUPABASE_ANON_KEY: "ISI_ANON_KEY_KAMU",
};
